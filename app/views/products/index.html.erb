<h1>Zayyad's Products</h1>

<%= link_to "New Product", new_product_path if authenticated? %>
<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, t("product_name", product: product) %>
    </div>
  <% end %>
</div>